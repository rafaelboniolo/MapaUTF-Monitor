import React from 'react'

import './Header.css'

export default function Header(){
    return(
        <div style={{display:'flex'}}>
            
            < div style={{display:'flex', backgroundColor:"grey", justifyContent:'center', flex:1}}>
                <h1 style={{display:'flex'}} >MapaUTF</h1>
            </div>
        </div>
    )
}

